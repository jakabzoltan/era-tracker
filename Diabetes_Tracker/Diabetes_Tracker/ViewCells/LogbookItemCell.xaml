<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Diabetes_Tracker.ViewCells.LogbookItemCell"
             IsEnabled="false"
          >
    <Grid Margin="15,10">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="6*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Column="0" VerticalOptions="CenterAndExpand" HorizontalOptions="Center" Font="Bold"  Text="{Binding GlucoseMeasurement.GlucoseConcentration}" TextColor="{Binding GlucoseTextColor}"  FontSize="24"/>
        <Label Grid.Column="1" VerticalOptions="CenterAndExpand" HorizontalOptions="End"  Text="{Binding GlucoseMeasurement.GlucoseConcenstrationUnits}" TextColor="{Binding GlucoseTextColor}"  FontSize="16"/>
        <StackLayout Grid.Column="2">
            
            <Label VerticalOptions="Center" HorizontalOptions="End" Text="{Binding StringTimeRecorded}" FontSize="14"/>
            <!--<Label VerticalOptions="Center" HorizontalOptions="End" Text="{Binding FeelingName}" FontSize="14"/>-->
            <!--<Label VerticalOptions="Center" HorizontalOptions="End" Text="{Binding FoodEaten}" FontSize="14"/>-->
        </StackLayout>
    </Grid>
</ViewCell>